import 'package:flutter/material.dart';

class AddActType extends StatelessWidget {
  const AddActType({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
