import 'package:flutter/material.dart';

class AddStartApp extends StatelessWidget {
  const AddStartApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
